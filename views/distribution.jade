extend layout

block content
  h2 #{title}

  h3 Distribution of #{title} among #{numeral(total).format('0,0')} apps

  div(class='barchart distribution')
  div(class='histogram')

  script(type='text/javascript').
    $(document).ready(function() {
      createHistogram(!{JSON.stringify(values)});
    });

